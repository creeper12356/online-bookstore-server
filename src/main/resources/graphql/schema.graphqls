type Query {
    bookByTitle(title: String): [Book]
}

type Book {
    id: Int
    title: String
    author: String
    description: String
    isbn: String
    price: Int
    cover: String
    sales: Int
    stock: Int
}